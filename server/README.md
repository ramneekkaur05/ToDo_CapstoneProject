## Todo Server

Scripts:

- `npm install`
- `npm run dev` (default port 4000)

Endpoints:

- GET `/api/health`
- GET `/api/todos`
- POST `/api/todos` `{ title }`
- PUT `/api/todos/:id` `{ title?, completed? }`
- DELETE `/api/todos/:id`


